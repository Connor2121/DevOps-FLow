[project]
name = "mcp-test-server"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "mcp[cli]>=1.2.0",
    "fastmcp>=0.2.0",
    "uvicorn>=0.30.0",
    "httpx>=0.27.0",
]

[project.scripts]
mcp-test-server = "app:main"

[tool.setuptools]
py-modules = ["app"]



# Sync code to your workspace
DATABRICKS_USERNAME=$(databricks current-user me | jq -r .userName)
databricks sync . "/Workspace/Users/$DATABRICKS_USERNAME/mcp-test-server"

# Create the app (name MUST start with "mcp-" to be recognized by AI Playground)
databricks apps create mcp-test-server

# Deploy it
databricks apps deploy mcp-test-server \
  --source-code-path "/Workspace/Users/$DATABRICKS_USERNAME/mcp-test-server"
