headers = {
    "Authorization": f"Bearer {token.token}",
    "Content-Type": "application/octet-stream"
}

TENANT    = "test"
SITE_PATH = "sites/FinanceLoan/test22"
FILENAME  = "test.pdf"

# Step 1 — get the drive ID for "Shared Documents"
drives_response = requests.get(
    f"https://graph.microsoft.com/v1.0/sites/{TENANT}.sharepoint.com:/{SITE_PATH}:/drives",
    headers=headers
)
